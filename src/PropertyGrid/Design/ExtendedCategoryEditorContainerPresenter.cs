using Avalonia.Controls;

namespace PropertyGrid.Design
{
    public class ExtendedCategoryEditorContainerPresenter : ContentControl
    {
        public Type StyleKey => typeof(ExtendedCategoryEditorContainerPresenter);

        /// <summary>
        /// Initializes a new instance of the <see cref="CategoryEditorContentPresenter"/>.
        /// </summary>
        public ExtendedCategoryEditorContainerPresenter()
        {
            
        }
    }
}